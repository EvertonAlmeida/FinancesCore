@using FinancesCore.App.Extensions
@model FinancesCore.App.ViewModels.TransactionViewModel

@{
    ViewData["Title"] = "Delete " + Model.Title;
}

<h4 style="padding-top: 50px">@ViewData["Title"] </h4>

<h5>Are you sure about that?</h5>

<vc:Summary></vc:Summary>

<div class="row">
    <div class="col-md-10">
        <dl>
            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Value)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Value)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Type)
            </dt>
            <dd>
                @this.GetTypeDescription(Model.Type)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Active)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Active)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Category)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Category.Title)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a class="btn btn-info" asp-action="Index">Back to List</a>
        </form>
    </div>
</div>
